#include<stdio.h>

/*
	特殊的指针
		1.void*		通用型指针
		2.NULL		指针初始化
*/

void* case_memset(void* s, int c, size_t n) 
{
	/*
		将传入的数组的前 n 个字节初始化为c;
	*/
}

int main48()
{	
	int* p = NULL;	// POINTER init
	return 0;
}
